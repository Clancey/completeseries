# AudiobookShelf Server Configuration
# Copy this file to .env and fill in your credentials

# Your AudiobookShelf server URL (required)
ABS_URL=https://your-audiobookshelf-server.com

# Option 1: Username/Password Authentication (default)
ABS_USERNAME=your_username
ABS_PASSWORD=your_password

# Option 2: API Key Authentication
# Uncomment and set these to use API key instead of username/password
# ABS_API_KEY=your_api_key
# ABS_USE_API_KEY=true

# Audible Region for looking up missing books
# Valid values: us, uk, ca, au, fr, de, jp, it, in, es, br
AUDIBLE_REGION=us

# Optional: Custom search URL for external search integration
# When set, a "Search" button will appear in book details
# The book title and author will be appended as a query parameter
# Example: SEARCH_URL=https://your-search-site.com/search
# SEARCH_URL=
